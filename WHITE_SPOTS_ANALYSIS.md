# 白斑問題根本原因分析 & 修復報告

## 🔴 問題症狀
生成的貼圖出現不明的白斑/白點，特別是在圖片邊緣和內容區域。

## 🎯 根本原因（已確認）

### 1. **背景檢測邏輯過於激進** ❌ 已修復
- 舊代碼：`r > 240` 會誤刪眼白、牙齒、衣服
- 舊代碼：`r > 200 && r < 240` 會誤刪膚色、頭髮
- 舊代碼：`r > 140 && r < 210` 會誤刪眼睛、嘴巴

### 2. **邊緣採樣點不足** ❌ 已修復
- 舊代碼：只採樣 8 個邊緣點
- 新代碼：採樣 20+ 個邊緣點（均勻分佈）

### 3. **背景檢測閾值過低** ❌ 已修復
- 舊代碼：`bgRatio < 0.5` 只要 50% 就觸發
- 新代碼：`bgRatio < 0.8` 需要 80% 才觸發

## ✅ 已實施的修復

### 修復 1: 更嚴格的背景檢測
```javascript
// ✅ 新代碼（第 464-476 行）
const isBackgroundColor = (r, g, b) => {
  const isPureWhite = r > 250 && g > 250 && b > 250;  // 只移除純白
  const isCheckerboardLight = r === 204 && g === 204 && b === 204;
  const isCheckerboardDark = r === 153 && g === 153 && b === 153;
  return isPureWhite || isCheckerboardLight || isCheckerboardDark;
};
```

### 修復 2: 增加邊緣採樣點
```javascript
// ✅ 新代碼（第 478-502 行）
// 四角 + 上下邊緣均勻採樣 + 左右邊緣均勻採樣
// 總計 20+ 個採樣點
```

### 修復 3: 提高觸發閾值
```javascript
// ✅ 新代碼（第 510-513 行）
if (bgRatio < 0.8) {  // 從 0.5 改為 0.8
  console.log(`    ⏭️ 邊緣非背景色（< 80%），跳過去背`);
  return imageBuffer;
}
```

## 📊 預期改進

| 項目 | 舊行為 | 新行為 |
|------|--------|--------|
| 眼白保留 | ❌ 被刪除 | ✅ 保留 |
| 牙齒保留 | ❌ 被刪除 | ✅ 保留 |
| 衣服亮部 | ❌ 被刪除 | ✅ 保留 |
| 頭髮邊緣 | ❌ 被刪除 | ✅ 保留 |
| 棋盤格移除 | ✅ 移除 | ✅ 移除 |
| 純白背景移除 | ✅ 移除 | ✅ 移除 |

## 🧪 測試建議

1. 生成包含眼白、牙齒的貼圖
2. 檢查是否有白斑出現
3. 確認棋盤格背景完全移除
4. 驗證邊緣清晰度

