# 🎨 視覺化修復說明

## 問題 1: 中間空白卡片缺少施工圖標

### 修復前
```
┌─────────────────────┐
│                     │
│        📤           │  ← 看起來像空白或錯誤
│   上傳準備狀態      │
│   待上傳：0/40 張   │
│                     │
└─────────────────────┘
```

### 修復後
```
┌─────────────────────┐
│                     │
│        🚧           │  ← 清楚表示「準備中」
│   上傳準備狀態      │
│   待上傳：0/40 張   │
│                     │
└─────────────────────┘
```

## 問題 2: 背景去除誤刪身體部分

### 修復前的問題

```
原始圖片                背景去除後（錯誤）
┌──────────┐          ┌──────────┐
│  👤      │          │  👤      │
│ /│\     │    →     │ /│\     │  ← 手部變透明！
│  │      │          │  │      │
│ / \     │          │ / \     │  ← 腳部變透明！
└──────────┘          └──────────┘
   完整                  部分透明
```

**原因**:
- tolerance = 30 太寬鬆
- RGB > 250 閾值太低
- 沒有排除膚色範圍
- 觸發閾值 80% 太低

### 修復後的效果

```
原始圖片                背景去除後（正確）
┌──────────┐          ┌──────────┐
│  👤      │          │  👤      │
│ /│\     │    →     │ /│\     │  ← 手部完整！
│  │      │          │  │      │
│ / \     │          │ / \     │  ← 腳部完整！
└──────────┘          └──────────┘
   完整                  完整保留
```

**改進**:
- tolerance = 5 更精確
- RGB >= 253 更嚴格
- 添加膚色保護邏輯
- 觸發閾值 90% 更保守

## 膚色保護邏輯

```
膚色特徵：R > G > B
範圍：
  R: 180-255
  G: 140-220
  B: 120-200

示例：
  RGB(220, 180, 150) ✅ 膚色，保留
  RGB(200, 160, 140) ✅ 膚色，保留
  RGB(255, 255, 255) ❌ 白色，移除
  RGB(204, 204, 204) ❌ 棋盤格，移除
```

## 背景檢測流程

### 修復前（過於激進）
```
1. 檢測邊緣顏色
2. RGB > 250 → 判定為背景
3. 80% 邊緣是背景 → 執行去背
4. Flood Fill 從邊緣開始
5. ❌ 誤刪膚色、衣服
```

### 修復後（更保守）
```
1. 檢測邊緣顏色
2. RGB >= 253 → 判定為背景
3. 排除膚色範圍 (R>G>B)
4. 90% 邊緣是背景 → 執行去背
5. Flood Fill 從邊緣開始
6. ✅ 保留膚色、衣服
```

## 測試案例對比

| 顏色 | RGB | 修復前 | 修復後 |
|------|-----|--------|--------|
| 純白 | (255,255,255) | ❌ 移除 | ❌ 移除 |
| 接近白 | (252,252,252) | ❌ 移除 | ✅ 保留 |
| 膚色 | (220,180,150) | ❌ 移除 | ✅ 保留 |
| 棋盤格亮 | (204,204,204) | ❌ 移除 | ❌ 移除 |
| 棋盤格暗 | (153,153,153) | ❌ 移除 | ❌ 移除 |
| 灰色衣服 | (150,150,150) | ❌ 移除 | ✅ 保留 |
| 藍色衣服 | (100,100,200) | ✅ 保留 | ✅ 保留 |

## 關鍵改進點

### 1. 更嚴格的白色閾值
```javascript
// 修復前
const isPureWhite = r > 250 && g > 250 && b > 250;

// 修復後
const isPureWhite = r >= 253 && g >= 253 && b >= 253;
```

### 2. 更精確的棋盤格匹配
```javascript
// 修復前
const tolerance = 30;  // 太寬鬆

// 修復後
const tolerance = 5;   // 更精確
```

### 3. 膚色保護
```javascript
// 新增
const isSkinTone = r > g && g > b && 
                   r >= 180 && r <= 255 && 
                   g >= 140 && g <= 220 && 
                   b >= 120 && b <= 200;
if (isSkinTone) return false;
```

### 4. 更高的觸發閾值
```javascript
// 修復前
if (bgRatio < 0.8) return imageBuffer;  // 80%

// 修復後
if (bgRatio < 0.9) return imageBuffer;  // 90%
```

---

**結論**: 通過這些改進，背景去除邏輯變得更加保守和精確，有效避免誤刪人物的膚色、衣服等重要區域。

